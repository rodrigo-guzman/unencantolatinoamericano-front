import SecondaryPages from '../../../../components/secondaryPages/SecondaryPages';

const AgendaPage = () => {
    return (
        <SecondaryPages componentName={'Agenda'} descriptionTitle={'AGENDA'} />
    );
}

export default AgendaPage;