import SecondaryPages from '../../../../components/secondaryPages/SecondaryPages';

const EventosPage = () => {
    return (
        <SecondaryPages componentName={'Eventos'} descriptionTitle={'EVENTOS'} />
    );
}

export default EventosPage;