import SecondaryPages from '../../../components/secondaryPages/SecondaryPages';

const RegionesLatAmPage = () => {
    return (
        <SecondaryPages componentName={'Regiones'} descriptionTitle={'Regiones'} />
    );
}

export default RegionesLatAmPage;